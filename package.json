{
  "name": "command-buttons",
  "displayName": "Command Buttons",
  "description": "Configurable buttons in the sidebar to send commands to the terminal, like SecureCRT's button bar.",
  "version": "0.1.0",
  "publisher": "skdong",
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:commandButtons.sidebar"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "commandButtons",
          "title": "Command Buttons",
          "icon": "$(terminal)"
        }
      ]
    },
    "views": {
      "commandButtons": [
        {
          "id": "commandButtons.sidebar",
          "name": "Buttons",
          "icon": "$(terminal)"
        }
      ]
    },
    "commands": [
      {
        "command": "commandButtons.refresh",
        "title": "Command Buttons: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "commandButtons.runButton",
        "title": "Command Buttons: Run Button",
        "icon": "$(play)"
      },
      {
        "command": "commandButtons.addButton",
        "title": "Command Buttons: Add Button",
        "icon": "$(add)"
      },
      {
        "command": "commandButtons.editButton",
        "title": "Command Buttons: Edit Button",
        "icon": "$(edit)"
      },
      {
        "command": "commandButtons.deleteButton",
        "title": "Command Buttons: Delete Button",
        "icon": "$(trash)"
      },
      {
        "command": "commandButtons.moveButtonUp",
        "title": "Command Buttons: Move Up",
        "icon": "$(arrow-up)"
      },
      {
        "command": "commandButtons.moveButtonDown",
        "title": "Command Buttons: Move Down",
        "icon": "$(arrow-down)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "commandButtons.addButton",
          "when": "view == commandButtons.sidebar",
          "group": "navigation@1"
        },
        {
          "command": "commandButtons.refresh",
          "when": "view == commandButtons.sidebar",
          "group": "navigation@2"
        }
      ],
      "view/item/context": [
        {
          "command": "commandButtons.runButton",
          "when": "view == commandButtons.sidebar && viewItem =~ /button/",
          "group": "inline"
        },
        {
          "command": "commandButtons.editButton",
          "when": "view == commandButtons.sidebar && viewItem =~ /button/",
          "group": "primary@1"
        },
        {
          "command": "commandButtons.deleteButton",
          "when": "view == commandButtons.sidebar && viewItem =~ /button/",
          "group": "primary@2"
        },
        {
          "command": "commandButtons.moveButtonUp",
          "when": "view == commandButtons.sidebar && viewItem =~ /button/",
          "group": "secondary@1"
        },
        {
          "command": "commandButtons.moveButtonDown",
          "when": "view == commandButtons.sidebar && viewItem =~ /button/",
          "group": "secondary@2"
        },
        {
          "command": "commandButtons.addButton",
          "when": "view == commandButtons.sidebar && viewItem =~ /group/",
          "group": "primary@1"
        }
      ]
    },
    "configuration": {
      "title": "Command Buttons",
      "properties": {
        "commandButtons.buttons": {
          "type": "array",
          "default": [],
          "title": "Buttons",
          "description": "Global button configuration. Each button sends text to the active terminal when clicked.",
          "items": {
            "type": "object",
            "title": "Button",
            "required": ["id", "label", "command"],
            "properties": {
              "id": {
                "type": "string",
                "title": "ID",
                "description": "Unique identifier for this button."
              },
              "label": {
                "type": "string",
                "title": "Label",
                "description": "Display text shown on the button."
              },
              "icon": {
                "type": "string",
                "title": "Icon",
                "description": "Optional Codicon ID (e.g. '$(gear)'). Leave empty for no icon."
              },
              "command": {
                "type": "string",
                "title": "Command",
                "description": "Text to send to the terminal (newline appended automatically)."
              },
              "group": {
                "type": "string",
                "title": "Group",
                "description": "Optional group name. Buttons in the same group are shown together under a collapsible section."
              },
              "tooltip": {
                "type": "string",
                "title": "Tooltip",
                "description": "Optional hover tooltip. Defaults to showing the command text."
              }
            }
          }
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "echo 'no linter configured'"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
